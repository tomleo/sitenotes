
Github Pages requires root directory to be the "output" so I need to create a
project with the "source" i.e. content, templates, requirements.txt and create
a submodule that links up with github that will be the github pages
